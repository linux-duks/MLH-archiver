name: Anonymizer Test

on:
  push:
      paths:
      - 'anonymizer/**'
  workflow_dispatch:

jobs:
  anonymizer-tests:
    uses: ./.github/workflows/py-tests.yaml
    name: anonymizer-tests
    with:
      path: ./anonymizer
      project-name: anonymizer


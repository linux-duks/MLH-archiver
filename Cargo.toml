[workspace]
resolver = "2"

members = ["mlh_archiver", "scripts/check_nntp", "scripts/check_git"]

[profile.release]
lto = "fat"
opt-level = 3
debug = false
panic = "abort"
incremental = false
codegen-units = 1
rpath = false
